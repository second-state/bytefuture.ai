---
export interface Props {
  title: string;
  summary: string;
  slug: string;
  lang: 'en' | 'zh' | 'ja' | 'ko';
  category: string;
  date: Date;
  cta: string;
  cover?: string;
  draft?: boolean;
}

const { title, summary, slug, lang, category, date, cta, cover, draft = false } = Astro.props;
const suffix = lang === 'en' ? '' : `-${lang}`;
const url = `https://bytefuture.ai/blog/${slug}${suffix}.html`;
// Covers are repo-root-relative in frontmatter (blog/foo.png); og:image needs an absolute URL.
const coverUrl = cover ? `https://bytefuture.ai/${cover.replace(/^\/+/, '')}` : undefined;
const locale = { en: 'en_US', zh: 'zh_CN', ja: 'ja_JP', ko: 'ko_KR' }[lang];
const htmlLang = { en: 'en', zh: 'zh-Hans', ja: 'ja', ko: 'ko' }[lang];
const writings = { en: 'Writings', zh: '文章', ja: '記事', ko: '글' }[lang];
const all = { en: '← All writings', zh: '← 所有文章', ja: '← すべての記事', ko: '← 모든 글' }[lang];
const recent = { en: 'Recent', zh: '最近', ja: '最近の記事', ko: '최근 글' }[lang];
const topics = { en: 'Topics', zh: '主题', ja: 'トピック', ko: '주제' }[lang];
const share = { en: 'Share this post', zh: '分享这篇文章', ja: 'この記事をシェア', ko: '이 글 공유하기' }[lang];
const copyLink = { en: 'Copy link', zh: '复制链接', ja: 'リンクをコピー', ko: '링크 복사' }[lang];
const copied = { en: 'Copied!', zh: '已复制！', ja: 'コピーしました！', ko: '복사됨!' }[lang];
const copyCode = { en: 'Copy', zh: '复制', ja: 'コピー', ko: '복사' }[lang];
const shareX = `https://x.com/intent/tweet?text=${encodeURIComponent(title)}&url=${encodeURIComponent(url)}`;
const shareLI = `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`;
const shareFB = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`;
const shareHN = `https://news.ycombinator.com/submitlink?u=${encodeURIComponent(url)}&t=${encodeURIComponent(title)}`;
const shareRD = `https://www.reddit.com/submit?url=${encodeURIComponent(url)}&title=${encodeURIComponent(title)}`;
const dateText = new Intl.DateTimeFormat(lang === 'zh' ? 'zh-CN' : lang === 'ja' ? 'ja-JP' : lang === 'ko' ? 'ko-KR' : 'en-US', { dateStyle: 'long' }).format(date);
---
<!DOCTYPE html>
<html lang={htmlLang}>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  {draft && <meta name="robots" content="noindex" />}
  <script>
  (function () {
    var LANGS = ['en', 'zh', 'ja', 'ko'];
    var path = location.pathname, slash = path.lastIndexOf('/');
    var dir = path.slice(0, slash + 1), file = path.slice(slash + 1) || 'index.html';
    var m = file.match(/^(.*?)(?:-(zh|ja|ko))?\.html$/i);
    var base = m ? m[1] : file.replace(/\.html$/i, '');
    var cur = (m && m[2]) ? m[2] : 'en';
    function target(l) { return dir + base + (l === 'en' ? '' : '-' + l) + '.html' + location.search + location.hash; }
    function cookie() { var h = document.cookie.match(/(?:^|;\s*)bf_lang=([^;]+)/); return h ? decodeURIComponent(h[1]) : ''; }
    window.bfLang = { cur: cur, pick: function (l) { if (LANGS.indexOf(l) === -1) return; document.cookie = 'bf_lang=' + l + ';path=/;max-age=31536000;samesite=lax'; if (l !== cur) location.href = target(l); } };
    var pref = cookie();
    if (LANGS.indexOf(pref) === -1) { pref = 'en'; var navs = navigator.languages || [navigator.language || '']; for (var i = 0; i < navs.length; i++) { var c = (navs[i] || '').slice(0, 2).toLowerCase(); if (c === 'zh' || c === 'ja' || c === 'ko' || c === 'en') { pref = c; break; } } }
    if (pref !== cur) { location.replace(target(pref)); }
    document.addEventListener('DOMContentLoaded', function () { var s = document.querySelector('select[data-bf-lang]'); if (s) s.value = cur; });
  })();
  </script>
  <title>{title} — ByteFuture {writings}</title>
  <meta name="description" content={summary} />
  <link rel="canonical" href={url} />
  <meta property="og:title" content={`${title} — ByteFuture ${writings}`} />
  <meta property="og:description" content={summary} />
  <meta property="og:type" content="article" />
  <meta property="og:url" content={url} />
  <meta property="og:locale" content={locale} />
  {coverUrl && (
    <>
      <meta property="og:image" content={coverUrl} />
      <meta property="og:image:width" content="1200" />
      <meta property="og:image:height" content="630" />
      <meta property="og:image:alt" content={title} />
    </>
  )}
  <meta name="twitter:card" content={coverUrl ? 'summary_large_image' : 'summary'} />
  <link rel="alternate" hreflang="en" href={`https://bytefuture.ai/blog/${slug}.html`} />
  <link rel="alternate" hreflang="zh-Hans" href={`https://bytefuture.ai/blog/${slug}-zh.html`} />
  <link rel="alternate" hreflang="ja" href={`https://bytefuture.ai/blog/${slug}-ja.html`} />
  <link rel="alternate" hreflang="ko" href={`https://bytefuture.ai/blog/${slug}-ko.html`} />
  <link rel="alternate" hreflang="x-default" href={`https://bytefuture.ai/blog/${slug}.html`} />
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-KQ0EX9QGK6"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-KQ0EX9QGK6');
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
  <style is:global>
    * { box-sizing: border-box; }
    html, body { max-width: 100%; overflow-x: clip; }
    body { background:#faf9f5; color:#18181b; font-family: Georgia, serif; margin:0; }
    a { color:#2563EB; overflow-wrap:anywhere; }
    body > nav { border-bottom:1px solid #e7e4da; background:#faf9f5; font-family:system-ui,sans-serif; }
    .nav-inner { height:64px; display:flex; align-items:center; justify-content:space-between; max-width:1200px; margin:0 auto; padding:0 24px; }
    .brand { display:flex; align-items:center; gap:9px; text-decoration:none; color:#18181b; font-weight:700; font-size:19px; letter-spacing:-0.02em; }
    .brand svg { display:block; flex:none; }
    .nav-actions { display:flex; align-items:center; gap:20px; }
    .nav-actions a { color:#52525b; text-decoration:none; font-weight:500; font-size:15px; }
    .nav-actions a:hover { color:#18181b; }
    .nav-actions select { border:1px solid #e4e4e7; border-radius:8px; padding:6px 8px; background:#fff; color:#18181b; font:inherit; font-size:14px; cursor:pointer; }
    .nav-actions select:hover { border-color:#2563EB; }
    .nav-actions .nav-cta { background:#2563EB; color:#faf9f5; padding:9px 16px; border-radius:9px; font-weight:600; font-size:14px; }
    .nav-actions .nav-cta:hover { background:#1d4ed8; color:#faf9f5; }
    .page-grid { display:grid; grid-template-columns:200px minmax(0,1fr) 200px; gap:48px; max-width:1200px; margin:0 auto; padding:0 24px; align-items:start; }
    .page-grid > * { min-width:0; }
    main, article, .prose { min-width:0; max-width:100%; }
    .sidebar-left,.sidebar-right { position:sticky; top:120px; padding-top:80px; font-family:system-ui,sans-serif; }
    .sidebar-left a,.toc a { display:block; font-size:13px; color:#71717a; text-decoration:none; padding:4px 0; line-height:1.5; }
    .sidebar-left h4 { font-size:13px; margin:24px 0 8px; }
    .article-header { padding:80px 0 40px; }
    .article-header h1 { overflow-wrap:anywhere; }
    .prose { font-size:18px; line-height:1.8; }
    .prose h2 { font-family:system-ui,sans-serif; font-size:28px; margin:56px 0 16px; scroll-margin-top:120px; overflow-wrap:anywhere; }
    .prose h3 { font-family:system-ui,sans-serif; overflow-wrap:anywhere; }
    .prose p,.prose ul,.prose ol { color:#27272a; margin:0 0 24px; }
    .prose .note { background:#fff; border:1px solid #e4e4e7; border-left:3px solid #2563EB; border-radius:12px; padding:16px 20px; margin:0 0 36px; font-family:system-ui,sans-serif; font-size:15px; line-height:1.65; color:#52525b; }
    .prose .note p { margin:0; color:inherit; }
    .prose .note p + p { margin-top:10px; }
    .prose figure { margin:32px 0; max-width:100%; overflow:hidden; }
    .prose img { display:block; width:100%; max-width:100%; height:auto; object-fit:contain; }
    .prose video { display:block; width:100%; max-width:100%; height:auto; border-radius:12px; border:1px solid #e4e4e7; }
    .prose figcaption { font-size:14px; color:#71717a; margin-top:10px; text-align:center; }
    .prose pre { background:#fff; border:1px solid #e4e4e7; border-radius:12px; padding:20px 24px; overflow-x:auto; margin:32px 0; max-width:100%; }
    .prose pre, .prose code { font-family:'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; font-size:0.9em; }
    .code-wrap { position:relative; margin:32px 0; }
    .code-wrap > pre { margin:0; }
    .copy-btn { position:absolute; top:10px; right:10px; z-index:1; display:inline-flex; align-items:center; gap:5px; border:1px solid #e4e4e7; background:#faf9f5; color:#52525b; border-radius:7px; padding:5px 10px; font-family:system-ui,sans-serif; font-size:12px; font-weight:500; cursor:pointer; transition:color .12s, border-color .12s; }
    .copy-btn:hover { color:#2563EB; border-color:#2563EB; }
    .copy-btn.copied { color:#0D9488; border-color:#0D9488; }
    .share { display:flex; align-items:center; flex-wrap:wrap; gap:10px; font-family:system-ui,sans-serif; margin:8px 0 8px; }
    .share-label { color:#71717a; font-size:15px; margin-right:2px; }
    .share-btn { display:inline-flex; align-items:center; gap:6px; border:1px solid #e4e4e7; background:#fff; color:#18181b; border-radius:50px; padding:8px 14px; font-size:13px; font-weight:500; text-decoration:none; cursor:pointer; transition:border-color .15s, color .15s; }
    .share-btn svg { flex:none; }
    .share-btn:hover { border-color:#2563EB; color:#2563EB; }
    .share-btn.copied { border-color:#0D9488; color:#0D9488; }
    .prose code { white-space:pre-wrap; overflow-wrap:anywhere; }
    .prose table { display:block; max-width:100%; overflow-x:auto; border-collapse:collapse; margin:32px 0; font-family:system-ui,sans-serif; font-size:15px; }
    .prose th,.prose td { border:1px solid #e4e4e7; padding:10px 14px; text-align:left; vertical-align:top; }
    .prose th { background:#f4f4f5; font-weight:600; }
    footer { max-width:1200px; margin:0 auto; padding:48px 24px; display:flex; justify-content:space-between; gap:24px; font-family:system-ui,sans-serif; color:#71717a; }
    @media (max-width:1024px){ .page-grid{grid-template-columns:1fr; max-width:760px;} .sidebar-left,.sidebar-right{display:none;} }
    @media (max-width:640px){ .nav-inner{padding:0 16px;} .nav-hide-sm{display:none;} .article-header{padding-top:48px;} .prose{font-size:17px;} footer{flex-direction:column;} }
  </style>
</head>
<body>
  <nav>
    <div class="nav-inner">
    <a href="/" class="brand"><svg width="26" height="26" viewBox="0 0 32 32" fill="none" aria-label="ByteFuture logo" role="img"><rect x="2.5" y="2.5" width="27" height="27" rx="8" fill="#2563EB" fill-opacity="0.10" stroke="#2563EB" stroke-width="1.6"/><circle cx="12" cy="16" r="3.4" fill="#2563EB"/><circle cx="21.5" cy="16" r="3.4" fill="none" stroke="#0D9488" stroke-width="1.8"/><path d="M15.4 16h2.7" stroke="#0D9488" stroke-width="1.8" stroke-linecap="round"/></svg><span>ByteFuture</span></a>
    <div class="nav-actions">
      <select data-bf-lang aria-label="Language" onchange="bfLang.pick(this.value)"><option value="en">EN</option><option value="zh">中文</option><option value="ja">日本語</option><option value="ko">한국어</option></select>
      <a href="/blog/" class="nav-hide-sm">{writings}</a>
      <a href="https://models.bytefuture.ai/intro.html" class="nav-cta">Token Station</a>
    </div>
    </div>
  </nav>
  <div class="page-grid">
    <aside class="sidebar-left"><a href="/blog/">{all}</a><h4>{recent}</h4><div id="recent-posts"></div><h4>{topics}</h4><div id="topics-list"></div></aside>
    <main>
      <header class="article-header"><div style="display:flex;gap:12px;align-items:center;margin-bottom:18px;"><span>{category}</span><time>{dateText}</time></div><h1>{title}</h1></header>
      <article class="prose"><slot /></article>
      <hr />
      <div class="share">
        <span class="share-label">{share}</span>
        <a class="share-btn" href={shareX} target="_blank" rel="noopener" onclick="gtag('event','share_click',{label:'x'})"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>Post</a>
        <a class="share-btn" href={shareLI} target="_blank" rel="noopener" onclick="gtag('event','share_click',{label:'linkedin'})"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.225 0z"/></svg>LinkedIn</a>
        <a class="share-btn" href={shareFB} target="_blank" rel="noopener" onclick="gtag('event','share_click',{label:'facebook'})"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>Facebook</a>
        <a class="share-btn" href={shareHN} target="_blank" rel="noopener" onclick="gtag('event','share_click',{label:'hackernews'})"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M0 24V0h24v24H0zM6.951 5.896l4.112 7.708v5.064h1.583v-4.972l4.148-7.799h-1.749l-2.457 4.875c-.372.745-.688 1.434-.688 1.434s-.297-.708-.651-1.434L8.831 5.896h-1.88z"/></svg>Hacker News</a>
        <a class="share-btn" href={shareRD} target="_blank" rel="noopener" onclick="gtag('event','share_click',{label:'reddit'})"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0zm5.01 4.744c.688 0 1.25.561 1.25 1.249a1.25 1.25 0 0 1-2.498.056l-2.597-.547-.8 3.747c1.824.07 3.48.632 4.674 1.488.308-.309.73-.491 1.207-.491.968 0 1.754.786 1.754 1.754 0 .716-.435 1.333-1.01 1.614a3.111 3.111 0 0 1 .042.52c0 2.694-3.13 4.87-7.004 4.87-3.874 0-7.004-2.176-7.004-4.87 0-.183.015-.366.043-.534A1.748 1.748 0 0 1 4.028 12c0-.968.786-1.754 1.754-1.754.463 0 .898.196 1.207.49 1.207-.883 2.878-1.43 4.745-1.487l.885-4.182a.342.342 0 0 1 .14-.197.35.35 0 0 1 .238-.042l2.906.617a1.214 1.214 0 0 1 1.108-.701zM9.25 12C8.561 12 8 12.562 8 13.25c0 .687.561 1.248 1.25 1.248.687 0 1.248-.561 1.248-1.249 0-.688-.561-1.249-1.249-1.249zm5.5 0c-.687 0-1.248.561-1.248 1.25 0 .687.561 1.248 1.249 1.248.688 0 1.249-.561 1.249-1.249 0-.687-.562-1.249-1.25-1.249zm-5.466 3.99a.327.327 0 0 0-.231.094.33.33 0 0 0 0 .463c.842.842 2.484.913 2.961.913.477 0 2.105-.056 2.961-.913a.361.361 0 0 0 .029-.463.33.33 0 0 0-.464 0c-.547.533-1.684.73-2.512.73-.828 0-1.979-.196-2.512-.73a.326.326 0 0 0-.232-.095z"/></svg>Reddit</a>
        <button type="button" class="share-btn" id="copy-link" data-copy={copyLink} data-copied={copied}><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M3.9 12c0-1.71 1.39-3.1 3.1-3.1h4V7H7c-2.76 0-5 2.24-5 5s2.24 5 5 5h4v-1.9H7c-1.71 0-3.1-1.39-3.1-3.1zM8 13h8v-2H8v2zm9-6h-4v1.9h4c1.71 0 3.1 1.39 3.1 3.1s-1.39 3.1-3.1 3.1h-4V17h4c2.76 0 5-2.24 5-5s-2.24-5-5-5z"/></svg><span class="cl-label">{copyLink}</span></button>
      </div>
    </main>
    <aside class="sidebar-right"><nav class="toc" id="toc"></nav></aside>
  </div>
  <footer><div>© ByteFuture</div><div>Hybrid AI inference for AI agents.</div></footer>
  <script>
    (function(){
      var toc=document.getElementById('toc'); if(toc){ document.querySelectorAll('.prose h2[id]').forEach(function(h){ var a=document.createElement('a'); a.href='#'+h.id; a.textContent=h.textContent; toc.appendChild(a); }); }
      var postsFile = (window.bfLang && window.bfLang.cur && window.bfLang.cur !== 'en') ? '/posts-' + window.bfLang.cur + '.json' : '/posts.json';
      fetch(postsFile).then(function(r){return r.json()}).then(function(posts){
        var recent=document.getElementById('recent-posts'); var topics=document.getElementById('topics-list');
        if(recent){ posts.slice(0,5).forEach(function(p){ var a=document.createElement('a'); a.href='/blog/'+p.slug+((window.bfLang&&window.bfLang.cur&&window.bfLang.cur!=='en')?'-'+window.bfLang.cur:'')+'.html'; a.textContent=p.title; recent.appendChild(a); }); }
        if(topics){ Array.from(new Set(posts.map(function(p){return p.category}))).forEach(function(c){ var a=document.createElement('a'); a.href='/blog/?cat='+encodeURIComponent(c); a.textContent=c; topics.appendChild(a); }); }
      }).catch(function(){});
    })();
  </script>
  <script define:vars={{ copyCode, copied }}>
    (function(){
      // Copy button on every code block (wrapped so the button stays pinned).
      document.querySelectorAll('.prose pre').forEach(function(pre){
        var wrap=document.createElement('div'); wrap.className='code-wrap';
        pre.parentNode.insertBefore(wrap, pre); wrap.appendChild(pre);
        var btn=document.createElement('button');
        btn.type='button'; btn.className='copy-btn'; btn.textContent=copyCode;
        btn.addEventListener('click', function(){
          var code=pre.querySelector('code'); var text=code?code.innerText:pre.innerText;
          navigator.clipboard.writeText(text).then(function(){
            btn.textContent=copied; btn.classList.add('copied');
            if(window.gtag) gtag('event','copy_code',{});
            setTimeout(function(){ btn.textContent=copyCode; btn.classList.remove('copied'); }, 1600);
          });
        });
        wrap.appendChild(btn);
      });
      // Copy-link share button (updates only the label span, keeps the icon).
      var cl=document.getElementById('copy-link');
      if(cl){ var lbl=cl.querySelector('.cl-label')||cl;
        cl.addEventListener('click', function(){
          navigator.clipboard.writeText(location.href).then(function(){
            lbl.textContent=cl.getAttribute('data-copied'); cl.classList.add('copied');
            if(window.gtag) gtag('event','share_click',{label:'copy_link'});
            setTimeout(function(){ lbl.textContent=cl.getAttribute('data-copy'); cl.classList.remove('copied'); }, 1600);
          });
        });
      }
    })();
  </script>
  <script data-goatcounter="https://bytefuture.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
</body>
</html>
